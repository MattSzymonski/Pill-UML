@start_style
// Override: use orange relationship lines instead of default
.relationship { stroke: #ff8800; }
.relationship-arrow { fill: #ff8800; stroke: #ff8800; }
@end_style

@start_uml
abstract class Entity {
    - id: u64
    + get_id(): u64
}

class Player {
    - name: String
    - health: i32
    + take_damage(amount: i32)
    + heal(amount: i32)
}

class Enemy {
    - damage: i32
    - speed: f32
    + attack(target: &Player)
}

interface Renderable {
    + render(&self)
    + get_bounds(): Rect
}

Entity <|-- Player
Entity <|-- Enemy
Renderable <|.. Player
Renderable <|.. Enemy
Player o-- Inventory
Enemy *-- AIController

class Inventory {
    - items: Vec<Item>
    + add_item(item: Item)
    + remove_item(index: usize)
}

class AIController {
    - state: AIState
    + update(dt: f32)
}
@end_uml
